import { HubContactPanel } from "../hub/contact-panel";
import { Layout } from "../shared/layout";

/** Partner hub — contact-only surface (Figma 692:11824). */
export function HubPage() {
  return (
    <Layout>
      <HubContactPanel />
    </Layout>
  );
}
